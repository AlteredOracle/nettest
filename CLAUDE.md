# NetPilot

NetPilot — Network diagnostics for Service Desk agents. The `app/` directory is
a static web app (deployed on Vercel); `main.js` + `electron-builder` package it
as an optional desktop app.

## Workflow preferences

- **Git: push directly to `main`.** Commit and push work straight to `main` for
  this project — no feature branch required.
